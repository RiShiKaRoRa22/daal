import java.util.*;

public class CityAndRoads {
    static final int inf= Integer.MAX_VALUE;

    public static void dijsktra(int n, List<List<int>> adj, int src){
        int dist[]= new int[n+1];
        boolean visited[]= new boolean[n+1];
        Arrays.fill(dist, inf);
        dist[src] = 0;

        for(int i=0;i<n;i++){
            int u=-1;
            
        }
        return;

    }
}
